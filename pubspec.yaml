import 'package:flutter/material.dart';

class AppConstants {
  // App Branding
  static const String appName = 'NOTES WALLAH';
  static const String appTagline = 'Learn Easy. Score Better.';

  // Colors
  static const Color primaryColor = Color(0xFF1E88E5); // Modern EdTech Blue
  static const Color accentColor = Color(0xFFFFB300);  // Gold / XP Color
  static const Color backgroundColor = Color(0xFFF8F9FA);
  static const Color cardColor = Colors.white;
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);

  // Default XP Values
  static const int xpPerChapterRead = 50;
  static const int xpPerChapterComplete = 100;
  static const int xpDailyStreakBonus = 20;

  // Placeholder Ad Slots for Future AdMob Integration
  static const String adSlotHomeTop = 'AD_SLOT_HOME_TOP';
  static const String adSlotNotesMid = 'AD_SLOT_NOTES_MID';
  static const String adSlotChapterList = 'AD_SLOT_CHAPTER_LIST';
}
